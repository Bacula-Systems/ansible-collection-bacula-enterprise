[DEFAULT]
user=<user_value>
fingerprint=<fingerprint_value>
key_file=/opt/bacula/etc/oci/oci_api_key.pem
tenancy=<tenancy_value>
region=<region_value>
pass_phrase=<pass_phrase_value>
[CLI]
# globally scoped default for all operations with a --compartment-id parameter
compartment-id = <value>
# globally scoped default for all operations with a --namespace parameter
namespace = <value>
[OCI_CLI_SETTINGS]
default_profile=CLI
